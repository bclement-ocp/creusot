use crate::{macros::*, std::default::Default};

// extern_spec! {
//    mod std {
//         mod alloc {
//             fn exchange_malloc(size: usize, align: usize) -> *mut u8;
//         }
//    }
// }
